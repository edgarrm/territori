# CONTEXT.md — Territori

> **Este archivo se carga al inicio de CADA sesión de Claude Code.** Es la fuente de verdad del proyecto. Si algo aquí contradice una instrucción suelta, gana este archivo (o se actualiza explícitamente).

---

## Qué es Territori

SaaS multi-tenant de **gestión territorial para campañas políticas** en México/LATAM. Cada campaña (tenant) elige un municipio, hereda sus secciones electorales del INE, y captura electores (contactos) georreferenciados por sección. El producto se vende por su **analítica territorial de cobertura en vivo** y su **gestión de brigadistas**.

**El dashboard central es un mapa** de secciones coloreado por cobertura (rojo = rezagada, verde = meta cumplida).

## Para quién

- **Brigadista**: captura en campo desde el celular, rápido. Rol `brigadista`.
- **Coordinador**: vive en el dashboard, reasigna esfuerzo. Rol `coordinador`.
- **Candidato/Dirección**: ve macro y proyección. (usa rol `coordinador` o `admin`).
- **Admin SaaS**: da de alta campañas. Rol `admin`.

---

## Stack

- **Backend**: Laravel 13 (PHP 8.3+) — starter kit oficial `laravel/vue-starter-kit`.
- **Auth**: **Laravel Fortify** (login, registro, reset, verificación email, 2FA, rate limiting). NO Jetstream (descontinuado). Fortify es solo backend; la UI es Vue/Inertia. Sobre Fortify montamos nuestro modelo users/memberships/tenant.
- **Frontend**: Vue 3 + Inertia.js v3 (SPA sin API REST separada para la app interna). Wayfinder para rutas tipadas.
- **DB**: PostgreSQL 16 + **PostGIS**. ⚠️ El starter kit viene con SQLite por defecto: hay que cambiar a pgsql en `.env` y `config/database.php` ANTES de migrar, o las migraciones PostGIS fallan.
- **Mapa**: Leaflet (GeoJSON servido por endpoint, geometría simplificada).
- **Calidad** (incluida en el starter kit): Pest (tests), Larastan/PHPStan (análisis estático), Pint (formato). Usar en el ciclo TDD.
- **Estilos**: (definir en Fase 03 — design tokens). Mobile-first responsive. Tailwind 4.
- **Colas**: jobs para agregados de cobertura (Redis/database queue).
- **Multi-tenancy**: por `tenant_id` (ADR-001), montado sobre el User de Fortify.

## Principios de arquitectura (ver _ai/adrs/)

1. **ADR-001 Multi-tenancy**: single-DB, `tenant_id` por columna, trait `BelongsToTenant` con global scope. `tenant_id` se resuelve del usuario autenticado, **NUNCA del request**. Catálogos INE (entidades/municipios/secciones) son **globales, sin tenant_id**.
2. **ADR-002 PostGIS**: geometrías en SRID 4326. Detección de sección por GPS con `ST_Contains`. Geometría servida al front siempre simplificada/cacheada.
3. **ADR-003 Captura unificada**: los 3 modos (lotería/individual/evento) escriben en `electores`, diferenciados por `modo_captura`. La cobertura del mapa se lee de la tabla derivada `cobertura_seccion`, **nunca** se agrega en vivo sobre `electores`.
4. **ADR-004 Privacidad**: datos personales bajo LFPDPPP. NO se captura intención de voto. Consentimiento obligatorio. Teléfono/domicilio cifrados; `telefono_hash` para dedup. Derechos ARCO soportados.
5. **ADR-005 White-label + facturación**: marca por tenant (`marca_nombre/logo/color/subdominio`), resolución de tenant por subdominio o tras login. Cobro por **brigadista activo** = membership con `rol=brigadista` y `activo=true`; `tenants.limite_brigadistas` por plan.

## Modelo de datos

Ver `_ai/docs/data-model.md` (autoridad). Entidad central: **`electores`** (tenant → sección → elector). Un elector tiene N **`interacciones`** (timeline; `tipo`=canal, `resultado`=qué pasó, separados). Metas en `metas_seccion` (manual o % de lista nominal). Agregados en `cobertura_seccion`.

**Identidad y pertenencia** (decidido): `users` = persona física (global, email único, sin tenant_id ni rol). `memberships` = pivote `(tenant_id, user_id, rol, meta_diaria, activo)`; una persona puede estar en varias campañas con distinto rol. `electores`/`loterias`/`interacciones` referencian `membership_id`, no `user_id`. Sin jerarquía de coordinadores (un solo nivel).

---

## Reglas para Claude Code en este repo

### Siempre
- **Spec primero**: ninguna feature se implementa sin su `_ai/specs/{feature}.spec.md`. Si no existe, créalo y pídelo validar antes de codear.
- Todo modelo de datos de campaña usa el trait `BelongsToTenant`. Si creas un modelo nuevo tenant-scoped y lo olvidas, es un bug.
- Toda query que toque `electores`/`interacciones` debe respetar el tenant scope. Prohibido `withoutGlobalScopes()` salvo en jobs de sistema documentados.
- Tests primero (TDD): el ciclo es Rojo → Verde → Refactor.
- Validación de teléfono y dedup por `telefono_hash` al crear electores.
- Consentimiento obligatorio: no se persiste un elector sin `consentimiento=true` y `aviso_privacidad_id`.

### Nunca
- Nunca aceptar `tenant_id` desde el cliente/request.
- Nunca leer el rol desde `users`: el rol vive en `memberships` y es por campaña.
- Nunca ligar una captura a `user_id` directo: usar `membership_id` (ata la persona al tenant correcto).
- Nunca ingerir nombres del padrón del INE (solo cartografía + conteo agregado de lista nominal).
- Nunca agregar `COUNT(*) GROUP BY seccion` sobre `electores` en el endpoint del mapa (usar `cobertura_seccion`).
- Nunca añadir campos de intención de voto o afiliación (fuera de alcance, riesgo legal).
- Nunca mezclar SRIDs; todo es 4326 de punta a punta.
- Nunca activar un brigadista por encima de `tenants.limite_brigadistas` sin manejar el caso (bloqueo/upsell).
- Nunca añadir una dependencia con rango `^`/`~`: fijar versión exacta. Usar `npm ci`/`composer install` (no `install`/`update` que reescriben lockfiles). No desactivar `ignore-scripts`/`min-release-age` del `.npmrc` (ver SETUP.md §8, seguridad de cadena de suministro).

### Convenciones
- Migraciones: una por tabla, con índices compuestos `(tenant_id, ...)` donde aplique.
- Eventos de dominio: `ElectorCapturado` dispara el job de actualización de `cobertura_seccion`.
- Comandos artisan de dominio prefijados `territori:` (ej. `territori:import-cartografia`, `territori:recalcular-cobertura`).

---

## Estado actual del proyecto

- **Fase**: 05/06 (SDD + Implementation), Sprint 7 cerrado — **plan de 7 sprints completo**.
- **Hecho**: PRD validado, ADRs 001-005, data-model, api-contract, demo de mapa (prototipo Leaflet con datos simulados — referencia visual, NO es el código final).
- **Sprint 1 (cartografía INE) — COMPLETO**: migraciones PostGIS (`entidades`, `municipios`, `secciones`, geom MultiPolygon 4326 + GiST), modelos Eloquent globales, `CartografiaSeeder` (`database/seeders/CartografiaSeeder.php`) con upsert idempotente. 10 tests en verde (`tests/Feature/CartografiaSeederTest.php`), incluido smoke test `@group slow` con dato real (Sinaloa clave 25, 20 municipios, 530 secciones de Mazatlán en `database/seeders/data/25-sinaloa/`). El comando `territori:import-cartografia` se decidió NO implementar por ahora — la carga se invoca manualmente con `(new \Database\Seeders\CartografiaSeeder)->run('database/seeders/data/{estado}')`.
- **Sprint 2 (auth + multi-tenancy + alta de campaña) — COMPLETO**: migraciones `tenants` (con campos white-label/límite) y `memberships` (único por tenant+user). `App\Models\Concerns\BelongsToTenant` + `TenantScope` (global scope que filtra por `App\Support\Tenancy\TenantContext`; sin tenant activo → cero resultados, nunca todos). Middleware `ResolveTenant` (subdominio vía `config('app.tenant_domain')`, o sesión) y `EnsureRol` (alias `rol:` en `bootstrap/app.php`, lee `currentMembership` vía `User::membershipEn()`, nunca `$user->rol`). `App\Http\Responses\LoginResponse` sustituye la respuesta default de Fortify: sin memberships → `campanas.sin-membership`; una → entra directo; varias → `campanas.seleccionar` (`CampanaSelectorController`). Acciones `App\Actions\Campana\CrearCampana` e `InvitarMiembro` (valida `limite_brigadistas` al activar brigadista). `Membership::activar()/desactivar()` registran `activado_en`/`desactivado_en`. Branding compartido en `HandleInertiaRequests` (`marca.nombre/color/logo_url`, fallback "Territori") e inyectado como `--brand` en `AppLayout.vue`. `DemoTenantSeeder` (manual, como CartografiaSeeder) crea tenant demo + admin/coordinador/brigadista sobre el primer municipio cargado. **BelongsToTenant aún no se usa en ningún modelo de producción** (se aplicará a `electores` y demás en Sprint 4); su test vive en `tests/Feature/Tenancy/BelongsToTenantTest.php` con un modelo de prueba ad-hoc. Suite de aislamiento + identidad + resolución + autorización + alta de campaña: 19 tests en verde en `tests/Feature/Tenancy/`.
- **Sprint 3 (mapa de cobertura + metas por sección) — COMPLETO**: migraciones `metas_seccion` (único `tenant_id+seccion_id`) y `cobertura_seccion` (PK compuesta `tenant_id+seccion_id`). Modelos `MetaSeccion` y `CoberturaSeccion`, ambos con `BelongsToTenant`. ⚠️ **`CoberturaSeccion` no soporta `save()`/`update()` de instancia** (PK compuesta rompe `setKeysForSaveQuery()` y generaba un `UPDATE` sin WHERE que afectaba todas las filas de la tabla — bug real encontrado y corregido en este sprint). Única vía de escritura: `CoberturaSeccion::upsertParaSeccion($seccionId, $atributos)`, que filtra explícito por `(tenant_id, seccion_id)`; `save()` lanza `LogicException` si la instancia ya existe. Acción `App\Actions\Metas\DefinirMetaSeccion` (manual o `lista_nominal_pct`, recalcula `cobertura_seccion`). Comando `territori:recalcular-cobertura {tenant}` (idempotente, capturados en 0 porque `electores` no existe aún). `App\Http\Controllers\MapaController`: `GET /api/cobertura.geojson` (geom simplificada con `ST_SimplifyPreserveTopology`, LEFT JOIN a `cobertura_seccion`), `GET /api/secciones/{seccion}/resumen`, `GET /metas` (Inertia, rol coordinador/admin), `PUT /api/secciones/{seccion}/meta` (rol coordinador/admin). `ResolveTenant` ya vivía en el stack `web` global desde Sprint 2 (no fue necesario agregar grupo nuevo, una suposición inicial del spec que se corrigió leyendo el código). Frontend: `Mapa.vue` (Leaflet — dependencia nueva, agregada con versión exacta tras confirmación) con selector cobertura/penetración y panel de detalle; `Metas.vue` con tabla editable. Bug preexistente de Sprint 1 corregido de paso: `App\Models\Entidad` no tenía `$table = 'entidades'` (Eloquent pluralizaba mal a `entidads`). Specs en `_ai/specs/mapa-cobertura.spec.md` y `.plan.md`. 24 tests nuevos en verde (`tests/Feature/MapaCobertura/`), suite completa en 94 tests verdes.
- **Sprint 4 (captura de electores: Lotería + Individual + dedup + consentimiento) — COMPLETO**: migraciones `avisos_privacidad`, `loterias` y `electores` (entidad central; `telefono`/`domicilio` con cast `encrypted` ADR-004, `telefono_hash` separado para dedup, `ubicacion` Point 4326 + GiST, `evento_id` columna nullable SIN FK porque `eventos` llega en Sprint 6, `modo_captura` solo `loteria|individual` por ahora). Modelos `Elector`/`Loteria`/`AvisoPrivacidad` con `BelongsToTenant`; `MembershipFactory` nueva (+ trait `HasFactory` en `Membership`). `App\Support\Telefono` normaliza (últimos 10 dígitos MX, null si <10) y hashea HMAC-SHA256 con `APP_KEY`. Acción `App\Actions\Electores\CapturarElector` (resuelve `membership_id` vía `User::membershipEn`, NUNCA del request; resuelve sección: lotería→heredada, individual→explícita o `ST_Contains` por GPS limitado al municipio; dedup por tenant → lanza `App\Exceptions\ElectorDuplicado` → controlador responde 409 con id existente; consentimiento obligatorio vía `StoreElectorRequest` regla `accepted`). `App\Actions\Loterias\AbrirLoteria` (una sola sesión abierta por brigadista; reabrir devuelve la existente). Evento `App\Events\ElectorCapturado` → listener `ActualizarCoberturaAlCapturar` (registrado explícito en `AppServiceProvider::boot`, NO auto-discovery) → job encolado `App\Jobs\ActualizarCoberturaSeccion` que fija `TenantContext` desde `tenant_id` (los jobs no heredan el currentTenant) y **recuenta** la sección (idempotente, no `+1`) vía acción compartida `App\Actions\Cobertura\RecalcularCoberturaSeccion`. `territori:recalcular-cobertura` ahora cuenta electores reales (ya no 0 hardcodeado) reusando esa misma acción. Controladores `ElectorController`/`LoteriaController`/`AvisoPrivacidadController`/`CapturaController`; endpoints `POST /api/electores`, `GET /api/electores/{elector}`, `GET /api/secciones/{seccion}/electores`, `POST/GET /api/loterias[...]`, `GET /api/avisos-privacidad/vigente`, página `GET /captura`. **⚠️ Bug de arquitectura encontrado y corregido: `SubstituteBindings` corre ANTES que `ResolveTenant` en el stack `web`, así que el binding implícito de un modelo tenant-scoped (`Elector`/`Loteria`) se resolvería SIN `TenantContext` (en prod → 404 al dueño legítimo; nunca fuga cross-tenant porque falla cerrada). Fix: resolver modelos tenant-scoped MANUALMENTE en el controlador (`Model::query()->findOrFail($id)`, corre tras `ResolveTenant`), nunca por type-hint de binding implícito. Regla nueva para futuros endpoints.** Frontend `Captura.vue` (mobile-first, lotería con contador en vivo + individual con GPS, aviso de privacidad, bloqueo por consentimiento). `DemoTenantSeeder` siembra un aviso vigente. 36 tests nuevos verdes (`tests/Feature/Captura/` + `tests/Unit/TelefonoTest.php`); suite completa **123 verdes**, Larastan limpio (de paso se añadieron genéricos a `BelongsToTenant::tenant()`). Specs en `_ai/specs/captura-electores.spec.md` y `.plan.md`.
- **Sprint 5 (brigadistas: gestión + zonas + ratios + facturación) — COMPLETO**: migración `brigadista_seccion` (pivote PK compuesta `membership_id+seccion_id`, con `tenant_id`; **sin modelo Eloquent con `save()`** por el bug de PK compuesta tipo `CoberturaSeccion`, se maneja vía `Membership::secciones()` belongsToMany con `sync` + payload `tenant_id`). ⚠️ **`Membership` NO usa `BelongsToTenant`** (intencional: se consulta para resolver el tenant mismo), así que TODA query de brigadistas/pivote filtra `tenant_id` **explícito**, y `{membership}` se resuelve manual (`Membership::query()->where('tenant_id',…)->where('rol','brigadista')->findOrFail`, regla anti-binding de Sprint 4). Acción `App\Actions\Brigadistas\AsignarZonas` (valida que las secciones sean del municipio del tenant → 422, idempotente vía `sync`). `App\Support\Brigadistas\RatiosBrigadista`: `paraTenant` (lista, **una** query agregada sobre `electores` con `COUNT(*) FILTER`, sin N+1) y `paraMembership`; ratios = `capturas_dia` (desde `now()->startOfDay()`, no `created_at::date=CURRENT_DATE` que mezclaba TZ y era frágil en medianoche), `capturas_total`, `pct_completos` (= electores con `telefono_hash`, decisión validada), `avance_meta` (`capturas_dia/meta_diaria`, null si sin meta — sin división por cero), `secciones_asignadas`/`secciones_ids`. `BrigadistaController` (`rol:coordinador,admin`): `GET /brigadistas` (Inertia: brigadistas+ratios + `facturacion {activos,limite,puede_activar}`), `POST /brigadistas` (reusa `InvitarMiembro`; límite → 422 con upsell), `PUT /api/brigadistas/{m}/activo` (reusa `Membership::activar/desactivar`; activar al tope → 422), `PUT /api/brigadistas/{m}/zonas`, `GET /api/brigadistas/{m}/ratios`. `MapaController::resumenSeccion` ahora **puebla `brigadistas_activos`** (activos Y asignados a la sección vía `brigadista_seccion`⨝`memberships`⨝`users`; inactivo o no-asignado no aparece). Frontend `Brigadistas.vue` (tabla con toggle activo, banner de límite/upsell, alta, modal de zonas reusando patrón de `Metas.vue`). `DemoTenantSeeder` asigna 5 secciones al brigadista demo. **Decisión validada: las zonas NO restringen la captura** (organizativas/visuales; `CapturarElector` de Sprint 4 sin cambios). Facturación = solo conteo+límite+bloqueo (snapshot de pico mensual fuera de alcance). Larastan nota: `Collection` es invariante, así que `paraTenant` devuelve `list<...>` (no `Collection<...>` desde `map()`, que falla invariancia aun con tipos idénticos). 15 tests nuevos verdes (`tests/Feature/Brigadistas/`); suite completa **138 verdes**, Larastan limpio. Specs en `_ai/specs/brigadistas.spec.md` y `.plan.md`.
- **Post-Sprint 5 (UI + demo, ya commiteado, NO es un sprint formal)**: navegación del sidebar por rol (`AppSidebar.vue` filtra Metas/Brigadistas a coordinador/admin; `HandleInertiaRequests` comparte `auth.rol` y `auth.tenant`). Rediseño de `Mapa.vue` tomando el prototipo `territori_demo.html` como referencia: panel izquierdo (header de marca, toggle Cobertura/Penetración/Tipo, stats globales calculadas client-side del GeoJSON, buscador por número, ficha rica con badge + barra), escala de **5 buckets** (Meta cumplida/Buen avance/En proceso/Rezagada/Desértica) y leyenda flotante; altura de la fila acotada a `100dvh-5rem` (header `h-16` + `m-2` del inset) para que el `aside` scrollee y la leyenda no se corte. `MapaController::index` (pasa municipio/estado/totalSecciones) y `resumenSeccion` enriquecido (`tipo`, `lista_nominal`, distritos y **`ultimo_registro`** ya poblado con el último elector). `DemoCapturasSeeder` (demo completo idempotente, inserción masiva: ~9.8k electores en las 530 secciones, 5 brigadistas, metas y distribución que cubre los 5 buckets). Nota: `penetración` sigue en 0 porque Mazatlán no trae `lista_nominal` del INE.
- **Sprint 6 (interacciones + agenda + edición de elector) — COMPLETO**: migración `interacciones` (`tenant_id`, `elector_id` FK cascade, `membership_id`, `tipo` `llamada|correo|visita|whatsapp|sms|nota`, `resultado` `contesto|no_contesto|buzon|correo_enviado|no_estaba|rechazo|compromiso` NULL, `nota`, `fecha` timestamptz, `proximo_seguimiento` date NULL, **`atendido_en` timestamptz NULL** — decisión nueva, no estaba en el data-model original, ya reflejada ahí; **sin `updated_at`**, solo `created_at`). Índices `(tenant_id,elector_id)` y `(tenant_id,membership_id,proximo_seguimiento)`. Modelo `App\Models\Interaccion` con `BelongsToTenant`, `const UPDATED_AT = null`, scope `pendientes()` (= `proximo_seguimiento <= hoy AND atendido_en IS NULL`); `Elector::interacciones()` hasMany desc por `fecha`. `App\Actions\Interacciones\RegistrarInteraccion` (membership SIEMPRE del servidor vía `User::membershipEn`, nunca del request; `tipo=nota` ⇒ `resultado` null; `fecha` default `now()`). `App\Actions\Electores\ActualizarElector` (edita `nombre/telefono/domicilio/observaciones`; si cambia el teléfono re-normaliza y re-hashea con `App\Support\Telefono`; colisión con OTRO elector del tenant → `ElectorDuplicado` → 409). `App\Http\Controllers\InteraccionController` (`indexPorElector`, `store`, `atendido` idempotente, `agenda` Inertia + `agendaData` JSON). **Decisión validada: agenda por rol** — brigadista ve solo sus seguimientos (`membership_id`), coordinador/admin ven todos los del tenant (rol leído de la membership activa, nunca de `users`). **Decisión validada: "atendido" explícito** vía `atendido_en` + `PUT /api/interacciones/{interaccion}/atendido`, no derivado. `ElectorController::update` (reusa `ActualizarElector`) y `ElectorController::page` (ficha Inertia con timeline). Endpoints: `GET/POST /api/electores/{elector}/interacciones`, `PUT /api/interacciones/{interaccion}/atendido`, `GET /agenda` + `GET /api/agenda`, `PUT /api/electores/{elector}`, `GET /electores/{elector}`. Resolución manual de modelos tenant-scoped (`Elector`/`Interaccion::query()->findOrFail`) — regla anti-binding de Sprint 4. FormRequests `StoreInteraccionRequest`/`UpdateElectorRequest` (authorize = membership en tenant → 403). Frontend: `Agenda.vue` (lista de pendientes, marcar atendido optimista con rollback, empty state, mobile-first), `Elector.vue` (ficha editable + timeline + form de nueva interacción con `resultado` condicional), entrada "Agenda" en el sidebar para todos los roles. `DemoCapturasSeeder` ahora siembra ~120 interacciones (40 pendientes vencidas) y limpia `interacciones` en su reset. Nota de TZ: `atendido_en`/`fecha` (timestamptz vía Eloquent) round-trip cambia la representación de offset (mismo patrón que `Loteria::cerrar` de Sprint 4); el test de idempotencia compara el valor persistido en DB, no la representación in-memory. 13 tests nuevos verdes (`tests/Feature/Interacciones/`); suite completa **151 verdes**, Larastan y Pint limpios. Specs en `_ai/specs/interacciones-agenda.spec.md` y `.plan.md`.
- **Sprint 7 (eventos + privacidad/ARCO + export) — COMPLETO**: migraciones `eventos` (`tenant_id`, `nombre`, `tipo`, `fecha`, `lugar`, `seccion_id` FK null, `ubicacion` Point 4326 + GiST), `solicitudes_arco` (`tenant_id`, `elector_id` FK null nullOnDelete, `tipo` `acceso|rectificacion|cancelacion|oposicion`, `estado` `pendiente|atendida`, `solicitado_en`, `atendido_en`), y `add_softdeletes_and_evento_fk_to_electores` que añade **`deleted_at`** (SoftDeletes) **y la FK pendiente** `electores.evento_id`→`eventos` (la columna existía nullable sin constraint desde Sprint 4). ⚠️ Las 3 migraciones comparten timestamp; la de electores se renombró a `230113` para correr **después** de `create_eventos` (la FK necesita la tabla). Modelos `Evento` (`BelongsToTenant`+`HasSpatial`, `seccion()`/`electores()`) y `SolicitudArco` (`BelongsToTenant`, `$timestamps=false`); `Elector` ahora `use SoftDeletes` + `evento()`. **Modo de captura `evento`**: `CapturarElector::resolverEvento` (evento del tenant, tenant-scoped) + sección **heredada del evento** (`evento.seccion_id`), fallback a sección explícita/GPS si el evento no tiene sede; `StoreElectorRequest` acepta `evento`+`evento_id`. `CrearEvento` (valida sección del municipio→422). **Privacidad/ARCO (decisiones validadas)**: borrado = **baja lógica** (`SoftDeletes`, NO físico), **solo coordinador/admin**; `CancelarElector` (transacción: **scrub de PII** — `nombre`/`telefono`→'' por NOT NULL, `telefono_hash`/`domicilio`/`ubicacion`/`observaciones`→null — + `softDelete` + registra `solicitudes_arco` `cancelacion/atendida` + recobertura de la sección reusando `RecalcularCoberturaSeccion`). `RegistrarSolicitudArco` (cualquier miembro, estado `pendiente`). Endpoints: `GET/POST /eventos` (Inertia, cualquier miembro; store devuelve `back()` → validación redirige con errores de sesión, no 422), `GET /api/eventos/{evento}/asistentes`, `POST /api/electores` (modo evento), `DELETE /api/electores/{elector}` (`rol:coordinador,admin`), `POST /api/solicitudes-arco` (cualquier miembro), `GET /api/export/electores.csv?seccion_id=&desde=&hasta=` (`rol:coordinador,admin`, `streamDownload`+`cursor()`, PII descifrada, anti-CSV-injection, excluye cancelados por el scope). SoftDeletes + BelongsToTenant son dos global scopes que conviven: dedup/listas/recobertura ya excluyen trasheados (re-captura del mismo teléfono tras cancelar funciona porque el hash se scrubbeó). Frontend: `Eventos.vue` (alta + lista + asistentes on-demand), tercer modo "Evento" en `Captura.vue`, botones ARCO en `Elector.vue` (registrar solicitud cualquiera; cancelar solo gestión), botón "Exportar CSV" con filtros de fecha en `Metas.vue`, sidebar con "Eventos". `DemoCapturasSeeder` siembra 2 eventos (30 asistentes marcando electores existentes para no alterar cobertura) + 1 solicitud ARCO pendiente; limpia `eventos`/`solicitudes_arco` en el reset. 15 tests nuevos verdes (`tests/Feature/Eventos/`, `Privacidad/`, `Export/`); suite completa **166 verdes**, Larastan y Pint limpios. Specs en `_ai/specs/eventos-arco-export.spec.md` y `.plan.md`. **Cierra el plan de construcción de 7 sprints.**
- **Preguntas abiertas RESUELTAS**: lista nominal = global (en `secciones`); brigadista multi-campaña = sí (users + memberships); jerarquía coordinadores = no (un nivel); white-label = sí (desde inicio, ADR-005); cobro = por brigadista activo (ADR-005). Único pendiente de producto: política exacta de cobro (pico mensual vs promedio vs corte).

## Próxima iteración: Análisis electoral por partido (post-demo 2026-07-08)

PRD y specs ya escritos y aprobados. Fuente: `_ai/docs/PRD-analisis-electoral-partido.md`. Implementar en este orden, un spec a la vez, ciclo completo (spec → tests → código → Pint):

1. **`_ai/specs/configuracion-campana.spec.md` (F1) — COMPLETO.** Ver detalle abajo. **Desbloquea F2 y F3.**
2. **`_ai/specs/competitividad-seccion.spec.md` (F2)** — PENDIENTE, empezar aquí. Estatus Ganada franca/Competida/Empatada/Perdida/SinDatos + diferencia de votos (perspectiva del partido del tenant) y tipo Alfa/Beta/Gama (por lista nominal); se integra en mapa (modo competitividad rehecho + modo nuevo Alfa/Beta/Gama), `/prioridades` (columnas/filtros/orden, índice neutral se mantiene pero toggleable) y detalle de sección.
3. **`_ai/specs/desglose-votos-seccion.spec.md` (F3+F4)** — PENDIENTE. Card "Votos por partido y coalición (2024)" en `Seccion.vue`, **entre** la card "Elección 2024" y "Perfil por edad" (posición exacta pedida por el cliente); backend agrupa `votos_partidos` por bloque en `resumenSeccion`; versión compacta en la ficha del mapa; mueve el botón "Agregar elector" al header de la card "Electores".

Dato clave: **no hay que re-importar nada** — `estadisticas_seccion.votos_partidos` ya trae las 24 opciones de boleta del CSV de Mazatlán 2024 (523 secciones, importado con `territori:importar-estadisticas`). Preguntas abiertas para el cliente (no bloquean F2/F3): si `brigadista` debe ver competitividad/desglose, y si los colores de partido propuestos (institucionales aproximados) le sirven o los quiere ajustar.

**F1 (configuración de campaña + catálogo electoral) — COMPLETO**: migraciones + modelos globales `Partido` (`siglas` unique, `nombre`, `color`) y `Coalicion` (tabla `coaliciones` — Eloquent pluraliza mal a `coalicions`, se fijó `protected $table` explícito, mismo patrón de bug que `Entidad` en Sprint 1; `partidos` jsonb array de siglas, único `(anio, nombre)`). `PartidoSeeder`/`CoalicionSeeder` idempotentes (`updateOrCreate`) con los 9 partidos y 2 coaliciones 2024 del PRD, llamados desde `DatabaseSeeder`. `tenants` +`partido_id` (FK nullable, `nullOnDelete`) +`settings` (jsonb nullable, cast `array`); `Tenant::configuracion()` hace `array_replace_recursive` de `settings` sobre defaults constantes de clase (umbral 30/1000/500, 4 indicadores en `true`) — un tenant sin settings se comporta con los defaults, aislado por tenant (no hay estado compartido). `App\Support\Bloques`: `agrupar()` (opción de boleta → bloque, dado un año de coaliciones) y `bloqueDePartido()` (bloque de un partido dado); `CAND_IND*` case-especial a "Independientes" (no se separa por `_` como el resto); reutilizable por F2/F3, sin dependencias de Eloquent en runtime (recibe `Collection<Coalicion>` ya cargada). `Settings\CampanaController` (`GET/PATCH /settings/campana`, protegido con el `rol:admin` existente dentro del grupo `tenant`, mismo patrón que `campanas.crear`) + `CampanaUpdateRequest` (`umbral_alfa` > `umbral_beta` vía closure; `authorize()` siempre `true` porque la autorización real la impone el middleware de ruta, patrón de `StoreBrigadistaRequest`). `HandleInertiaRequests` ahora comparte `campana` (partido + configuración efectiva) para que Mapa/Prioridades/Seccion (F2/F3) lo consuman sin fetch extra — `null` si no hay tenant activo. Frontend `settings/Campana.vue` (select de partido con swatch de color vía `useForm`, inputs numéricos de umbrales, checkboxes de indicadores) + entrada "Campaña" en `layouts/settings/Layout.vue` condicionada a `auth.rol === 'admin'`. Verificado end-to-end en navegador (login como admin demo, seleccionar partido, guardar, recargar, confirmar persistencia en BD) — ojo: un `npm run build` a mitad de sesión invalida la versión de Inertia cargada en el navegador y la siguiente request devuelve 409 + reload automático (comportamiento esperado, no bug). 34 tests nuevos verdes (`tests/Unit/BloquesTest.php`, `tests/Feature/Campana/`); suite completa **332 verdes**, Larastan y Pint limpios. Nota de entorno: el `php` del PATH global apunta a 8.1 fuera de la carpeta del proyecto — Herd resuelve 8.5 vía site-isolation dentro de `/Users/edgarrealmorales/Herd/territori`, así que los comandos artisan deben correr con el cwd en el proyecto (o `php85` explícito de Herd si hace falta desde otro cwd).

## Orden de construcción (sprints)

1. Cartografía INE: preparación local (shapefile→GeoJSON 4326) + `CartografiaSeeder` PHP a PostGIS. Prod sin GDAL/Python.
2. Auth + identidad (`users`) + multi-tenancy (`memberships`, resolución de tenant) + alta de campaña (municipio + marca white-label).
3. Mapa de cobertura (lee `cobertura_seccion`) + metas por sección.
4. Captura Lotería + Individual + validación/dedup + consentimiento.
5. Brigadistas (memberships) + asignación de zonas + ratios + límite/facturación por activo.
6. Interacciones (timeline) + agenda de seguimientos + edición de elector.
7. Eventos (modo captura `evento` + FK pendiente) + privacidad/ARCO (borrado + `solicitudes_arco`) + export CSV.

## Referencia de la demo

El prototipo en `territori_demo.html` muestra la UX objetivo del mapa, la ficha de sección, la ficha de elector y el timeline de interacciones. Sirve como **referencia de diseño e interacción**, no como código a portar tal cual.
